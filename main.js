var typed= new Typed(".text", {
    strings: ["Front End Developer", "Web Developer", "Python Developer"],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true
    })

    // project start

    document.querySelector('.project-link').addEventListener('click', function() {
        alert('You are about to view the project!');
    });

    // project end